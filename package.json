{
  "name": "fleet-management-monorepo",
  "private": true,
  "scripts": {
    "dev": "npx concurrently \"npm --prefix backend run dev\" \"npm --prefix 'Frontend Module Breakdown' run dev\"",
    "start": "npm --prefix backend run start",
    "install-all": "npm install && cd backend && npm install && cd ../\"Frontend Module Breakdown\" && npm install",
    "seed": "npm --prefix backend run seed"
  },
  "dependencies": {
    "express-session": "^1.19.0",
    "passport": "^0.7.0",
    "passport-google-oauth20": "^2.0.0",
    "react-router-dom": "^7.13.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
